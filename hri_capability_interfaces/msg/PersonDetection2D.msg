# Confidence assigned by the selected perception provider, in the [0.0, 1.0] range.
float64 confidence
# Stable provider tracking identifier when available; empty for untracked detections.
string tracking_id
# Bounding-box center and size in pixels in the provider's source image.
float64 center_x
float64 center_y
float64 width
float64 height
